% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model_assessment.R
\name{plot_model_assessment}
\alias{plot_model_assessment}
\title{Plot model assessment over time from metrics CSV}
\usage{
plot_model_assessment(
  data_file_path,
  time_column,
  facet_column = NULL,
  cbp_threshold = 0.001,
  separate_cbp = TRUE
)
}
\arguments{
\item{data_file_path}{Path to \code{Model_Assessment_Metrics.csv}.}

\item{time_column}{Single column name used on the x axis (for example, \code{"Year"}).}

\item{facet_column}{Optional single column name for faceting (for example, \code{"Month"}).}

\item{cbp_threshold}{Numeric threshold for CBP classification, defaults to \code{0.001}.}

\item{separate_cbp}{Logical, if \code{TRUE} draws separate CBP plots for G and E, otherwise a combined plot.}
}
\value{
Invisibly returns a list with the created plots and a summary table:
  \code{volume_sensitivity_plot}, \code{tp_fn_plot}, and either
  \code{cbp_g_plot} and \code{cbp_e_plot} or \code{cbp_combined_plot},
  plus \code{summary_stats}. Plots are printed to the active device.
}
\description{
Reads a \code{Model_Assessment_Metrics.csv} file and produces:
  (1) a combined plot of G-space hypervolume size and sensitivities over time
  with a dual y-axis, (2) a TP vs. FN plot in G-space, and (3) CBP plots
  either separated for G and E or combined, on a log scale with a threshold line.
}
\details{
The function expects the CSV to include at least:
\code{Fold}, \code{G_volume}, \code{sensitivity_test_G}, \code{sensitivity_test_E},
\code{CBP_test_G}, \code{CBP_test_E}, \code{TP_test_G}, \code{FN_test_G},
\code{TP_test_E}, \code{FN_test_E}, and the chosen \code{time_column}.
If \code{facet_column} is provided, summaries are computed within facets.
}
\seealso{
\code{\link{generate_spatiotemporal_predictions}} for producing the metrics CSV.
}
